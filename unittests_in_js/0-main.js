// test module for 0-calcul.js

const calculateNumber = require('./0-calcul');

// Example usage
const a = 2.7;
const b = 5.3;
const sum = calculateNumber(a, b);

console.log(`Rounded sum of ${a} and ${b} is: ${sum}`);
